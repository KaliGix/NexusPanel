/**
 * UI.js
 * Responsabilidad: Renderizar elementos en el DOM
 * No hace fetch, solo manipula HTML
 */

const UI = {
    /**
     * Renderiza las tarjetas del dashboard
     */
    renderCards(data) {
        
    },

    /**
     * Muestra estado de carga
     */
    showSpinner() {
     
    },

    /**
     * Muestra mensaje de error
     */
    showError(message) {
       
    },

    /**
     * Actualiza el título
     */
    updateTitle(title) {
      
    }
};
